# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Firebase Auth Demo (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Firebase/Auth'
  pod 'FirebaseUI/Auth'
  pod 'FirebaseUI/Email'
  

# Pods for Firebase Auth Demo (iOS)

end

target 'Firebase Auth Demo (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Firebase Auth Demo (macOS)

end
